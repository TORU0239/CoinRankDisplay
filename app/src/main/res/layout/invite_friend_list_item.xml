<?xml version="1.0" encoding="utf-8"?>
<androidx.cardview.widget.CardView xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:app="http://schemas.android.com/apk/res-auto"
    xmlns:tools="http://schemas.android.com/tools"
    android:layout_width="match_parent"
    android:layout_height="wrap_content"
    android:layout_marginTop="16dp"
    android:layout_marginStart="16dp"
    android:layout_marginEnd="16dp"
    app:cardElevation="0dp"
    app:cardBackgroundColor="@color/inviteFriendBgColor"
    app:cardCornerRadius="10dp"
    app:contentPadding="16dp">

    <androidx.constraintlayout.widget.ConstraintLayout
        android:layout_width="match_parent"
        android:layout_height="wrap_content">

        <ImageView
            android:id="@+id/imgInvite"
            android:layout_width="40dp"
            android:layout_height="40dp"
            android:padding="10dp"
            android:src="@drawable/invite_icon"
            android:background="@drawable/oval_bg"
            android:layout_marginTop="10dp"
            android:contentDescription="@null"
            app:layout_constraintStart_toStartOf="parent"
            app:layout_constraintTop_toTopOf="parent"/>

        <Space
            android:id="@+id/spaceImgInvite"
            android:layout_width="40dp"
            android:layout_height="10dp"
            app:layout_constraintTop_toBottomOf="@id/imgInvite"
            app:layout_constraintStart_toStartOf="@id/imgInvite"/>

        <TextView
            android:id="@+id/tvCoinName"
            android:layout_width="0dp"
            android:layout_height="0dp"
            android:layout_marginStart="16dp"
            android:textSize="16sp"
            android:textColor="@color/coinItemTextColor"
            android:includeFontPadding="false"
            android:text="@string/invite_friend"
            app:layout_constraintStart_toEndOf="@id/imgInvite"
            app:layout_constraintTop_toTopOf="@id/imgInvite"
            app:layout_constraintEnd_toEndOf="parent"
            app:layout_constraintBottom_toBottomOf="@id/imgInvite"/>
    </androidx.constraintlayout.widget.ConstraintLayout>

</androidx.cardview.widget.CardView>